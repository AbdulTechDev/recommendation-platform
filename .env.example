POSTGRES_DB=recommendationdb
POSTGRES_USER=recommendation_user
POSTGRES_PASSWORD=RecommendationDbPass2026
API_HOST_PORT=5050
# Do NOT commit real secrets. Provide a secure JWT key via your CI or secret manager.
# Example placeholder below — replace in your environment or CI with a secure value.
JWT_KEY=your_jwt_key_here_replace_in_ci

# Local override: create a `.env` file (which is gitignored) and set the real values
# or export environment variables in your shell before running `docker compose up`.# Copy this file to .env and fill in real values before running `docker compose up`.
# .env is gitignored — never commit real credentials.
# Note: POSTGRES_PASSWORD is interpolated directly into a postgresql:// URL for the
# recommendation-ai/seed services, so avoid characters that need URL-encoding (@ : / %).
POSTGRES_DB=recommendationdb
POSTGRES_USER=recommendation_user
POSTGRES_PASSWORD=change-me
