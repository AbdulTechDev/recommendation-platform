# Documentation for Recommendation Platform


 Next steps: fill each file with project-specific details, add diagrams to `docs/diagrams/`, and generate OpenAPI/Swagger for `Recommendation.Api`.

 Swagger / OpenAPI
 - The API already includes Swagger generation support via Swashbuckle. Run the `Recommendation.Api` project in `Development` environment and open `/swagger` to view the OpenAPI UI.

 ER Diagram
 - See `docs/diagrams/erd.svg` for a placeholder ER diagram. Replace with an export from your preferred modeling tool for production documentation.

Next steps: fill each file with project-specific details, add diagrams to `docs/diagrams/`, and generate OpenAPI/Swagger for `Recommendation.Api`.
